```
cat /var/log/nginx/error.log  # show error message to make sure the problem
ps -ef | grep nginx # list all the nginx process
sudo chown -R www-data:www-data /root/code/home2/  # give the process the permission for the target folders


whoami
su www-data
namei -l www/

```
